@using (Html.BeginForm())
{

    Html.DevExpress().UploadControl(
    ucsettings =>
    {

        ucsettings.Name = "ucCorrespondenceEmailCustomUploadData";
        ucsettings.CallbackRouteValues = new { Controller = "BulkCorrespondence", Action = "CallbacksCorrespondenceEmailCustomFileUpload" };
        ucsettings.ClientSideEvents.FileUploadComplete = "function(s, e) { QueueEmailList(e); }";
        ucsettings.ControlStyle.CssClass = "editorArea";
        ucsettings.Width = 200;
        ucsettings.ShowProgressPanel = true;
        ucsettings.BrowseButton.Text = ". . .";
//ucsettings.AdvancedModeSettings.EnableMultiSelect = true;
ucsettings.UploadMode = UploadControlUploadMode.Advanced;
    }).Render();


}
