@Html.DevExpress().GridView(settings =>
{
    GridUtils.SetGridViewProperties(settings, settings.CommandColumn, ViewData["ViewProperties"]);
    settings.ClientSideEvents.FocusedRowChanged = "OnGridViewFocusedRowChanged";

    

}).Bind(Model).GetHtml()