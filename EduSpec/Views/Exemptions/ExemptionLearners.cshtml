@Html.DevExpress().GridView(settings =>
{
    GridUtils.SetGridViewProperties(settings, settings.CommandColumn, ViewData["ExemptionLearnersViewProperties"]);
    settings.Width = 550;

}).Bind(Model).GetHtml()